/**
 * Tailwind CSS configuration
 * - We only customize dark mode to use the `class` strategy so we can toggle themes programmatically.
 * - Tailwind v4 auto-detects content; no `content` key needed.
 */
export default {
  darkMode: 'class',
};


